# 安全说明

## 报告漏洞

**请不要开公开 issue。**

用 GitHub 的私密漏洞报告：仓库 → Security → Report a vulnerability。
如果这条通道不可用，请发邮件给维护者（见仓库主页的联系方式），
标题以 `[SECURITY]` 开头。

请在报告里包含：

- 影响范围（例如：可被第三方来源触发、可泄露本地信息、影响构建产物）
- 复现步骤
- 你认为的严重程度
- 如果有，附上 PoC

我们会在 7 天内确认收到，并在修复后致谢（除非你希望匿名）。

## 这个项目的威胁模型

理解威胁模型才能判断什么算漏洞。Radar 是一个**静态站点生成器**，攻击面有三块：

### 1. 构建期的采集（主要风险）

`radar/collect.py` 会主动访问第三方 URL。这些页面是**不可信输入**。

已经施加的防护：

| 风险 | 防护 | 位置 |
| --- | --- | --- |
| SSRF：来源把请求指向内网 | 方案白名单（仅 http/https）+ 字面主机检查 + **DNS 解析后逐地址复检** | `radar/safety.py` |
| 云元数据端点窃取 | `169.254.169.254` 等元数据主机显式拒绝 | `radar/safety.py` |
| 通过域名白名单绕过 | 匹配要求 `host == entry` 或 `host.endswith("." + entry)`，`notgroq.com` 不会匹配 `groq.com` | `radar/safety.py` |
| DNS rebinding | 解析后复检每一个返回地址，只要有一个落在内网就拒绝 | `radar/safety.py` |
| 解压炸弹 | 解压后体积上限 5 MiB | `config/sources.yaml` |
| 无限重定向 | 最多 4 跳 | `config/sources.yaml` |
| 抓取脚本执行 | 只解析文本，**从不执行**来源页面的任何脚本 | `radar/parsers/` |

> **`FA_RADAR_ALLOW_DNS_PROXY=1`** 会放开 `198.18.0.0/15` 等 DNS 代理假地址段，
> 用于 Clash/Surge 环境。它**默认关闭**，且只放宽假地址段——回环、RFC1918、
> 链路本地、元数据地址始终阻断。不要在 CI 里无条件开启。

### 2. 构建期的模板渲染

来源里的文本最终会渲染进 HTML。**这是注入风险所在。**

- 服务端（Jinja2）用 `select_autoescape`。
- 客户端**从不用 `innerHTML` 渲染抓来的数据**，一律走 `render.js` 的 DOM 构建函数。
- Markdown 渲染器解析成 DOM，**从不拼成 HTML 字符串**。
- 只有 `http(s)` 链接会变成 `<a>`，`javascript:` 与 `data:` 被拒绝。
- 证据链接走 `check_evidence_link`，方案受限。

### 3. 运行期（站长之外的第三方）

站点是纯静态的，浏览器只读公开 JSON。**页面不会替访客发起任何出站请求**——
这是设计约束，不是现状描述。如果某次改动引入了第三方请求（分析、字体 CDN、
头像代理），那是一个需要讨论的架构变更，而不是小修补。

## 明确不做的承诺

- Radar **不**验证第三方服务的条款合规性。
- Radar **不**保证来源页面的内容无害。它只保证不执行来源的脚本、不把来源文本
  当 HTML 注入。
- Radar **不**用你的密钥发起任何请求。调用状态字段的默认值永远是"未测试"。

## 密钥处理

**密钥绝不出现在**：URL 查询串、导出文件、遥测、日志、构建产物。

强制手段：

- `radar/export_public.py` 用**字段白名单**导出——内部状态默认不公开，
  新增字段不会自动进公开数据集。
- `tests/scan_secrets.py` 扫描 `dist/`，发现形似密钥的字符串就让 CI 失败。
- 测试套件断言 CC Switch 载荷里 `apiKey` 永远为空值。
- `.gitignore` 排除 `.work/` 与本地状态文件。

如果你**发现构建产物里出现了密钥**，按漏洞处理，不要开公开 issue。

## 支持的版本

只有 `main` 分支的最新提交在支持范围内。
